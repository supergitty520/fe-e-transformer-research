# 数据与制品说明

**中文** | [English](DATA_AND_ARTIFACTS_EN.md)

## 数据性质

实验数据由程序生成，不包含用户数据、个人资料或第三方受限数据集。`results/`中的JSON和
JSONL记录模型配置、随机种子、逐步任务指标、传播诊断、介入事件、耗时与终止原因。

## 目录角色

- `results/**/logs/*.jsonl`：逐步原始证据，体积最大，视为不可变记录。
- `results/**/runs/*.json`：单次运行摘要。
- `results/**/manifest.json`：冻结配置、环境与运行清单。
- `results/*_analysis.json`：由分析脚本生成的结构化审计。
- `docs/`：人工可读实验报告、方法和停止说明。
- `output/figures/`：论文用PNG与可编辑SVG。
- `output/pdf/`：预印本PDF。
- `output/attachments/`：Release附件策略说明；生成的ZIP及SHA-256不进入主Git历史。
- `paper/`：可编辑预印本正文。
- `website/`：互动示意网页源码；依赖目录不提交。

## 仓库存储策略

清理开发期中间数据后，主候选集中的原始JSONL约185 MiB，最大单文件约9 MiB，低于
GitHub 100 MB单文件限制。私有研究仓库
可以直接版本化这些冻结日志，不依赖Git LFS。为避免Git历史膨胀：

- 已完成的JSONL不做原地重写；
- 修正分析逻辑时新增分析结果，不修改原始轨迹；
- 大型新实验若超过50 MB/文件，改用GitHub私有Release附件或对象存储；
- 不提交 `node_modules`、缓存、临时PDF渲染和博客部署副本。

## 失效与停止数据

具有可解释机制或固定更新数证据的主动停止运行保留在主候选集，并带停止审计。纯冒烟、
被替代校准、协议失效预运行和没有独立结论的中断轨迹保留在本地忽略归档，只在报告中保留
运行标识、排除理由和必要摘要。两类都不能进入正式均值、胜负或显著性汇总。不得以清理
为由删除已被研究结论引用的负证据。

## 隐私与机器信息

部分manifest包含本机后端、版本和绝对工作路径，用于环境追溯。私有协作阶段可以保留；
转为公开仓库前，应生成不改变实验数值的净化副本，把个人路径替换为相对项目路径，并
重新计算附件清单。`.secrets/`、SSH脚本和博客发布基础设施永不进入研究仓库。

## 校验

正式Release附件使用相邻 `.sha256` 文件，但附件本身不进入主Git历史。原始日志完整性由
对应分析脚本检查步号、开始/结束记录、介入表、验证间隔和failure记录。任何手工修改过的
结果都必须另存并说明来源。
