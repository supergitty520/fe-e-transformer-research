# 参与研究

**中文** | [English](CONTRIBUTING_EN.md)

本仓库处于私有研究预览阶段。贡献目标是提高可证伪性和复现性，而不是只寻找正结果。

## 提交实验前

Issue中必须写明：

- 要证伪的假设；
- 方法、基线和同构sham；
- 冻结的种子、任务、模型和验证批次；
- 主终点、次终点、安全上限和停止条件；
- 预计计算量、显存和墙钟时间；
- 哪些结果会使该方向停止。

调参使用开发种子，确认实验使用未参与选择的种子。看到结果后不得更换主终点。

## 结果提交

Pull Request至少包含：

- 配置或manifest；
- 原始逐步日志或其受控归档位置；
- 可从日志重建结果的分析脚本；
- 正结果、负结果、失败和主动停止；
- 固定更新数与墙钟成本；
- 首次介入前轨迹匹配和系统负载说明。

不要手工编辑原始JSONL。分析错误通过新增脚本或派生文件修正。

## 代码规范

- Python 3.10+；
- 新算法必须配测试；
- 不使用detached梯度伪装可训练正则；
- 不把微型合成任务结果写成大模型结论；
- 文档中的表格必须能追溯到结果文件或分析脚本。
- 主张、数字、终点或证据等级变化必须同步更新
  [中英文发布对应表](docs/bilingual_publication_map.md)中的成对文档。

## 安全

禁止提交密钥、SSH材料、个人数据、机器管理员脚本、博客部署凭据和第三方受限数据。发现
问题时按 `SECURITY.md` 处理，不要在Issue中公开秘密值。
